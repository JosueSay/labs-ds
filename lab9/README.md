# Uso de docker

Para levantarlo el container:

```bash
docker compose up
```

Reconstruir con los nuevos cambios del Dockerfile o `requirements.txt`:

```bash
docker compose up --build
```

Limpiar los contenedores creados:

```bash
docker compose down
```
